package FileProcessingApp;

import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class App {
 
	public static void main(String[] args) {
		
		
		FileReaderGUI gui = new FileReaderGUI();
		/*Scanner input = new Scanner(System.in);
		System.out.println("Text file path:");
		String filePath = new String(input.nextLine());
		File file = new File(filepath);
		FileReader fl = new FileReader(file);
		fl.swapWordsFromIndex(1, 2, 2, 1);
		
		System.out.println("Enter 1 to swap a line with another line.");
		System.out.println("Enter 2 to swap a word from an index with another one.");
		int userInput = input.nextInt();
		switch(userInput) {
			case 1:
				
				System.out.println("First line to swap.");
				int input1=input.nextInt(); //add better names
				System.out.println("Second line:");
				int input2=input.nextInt();
				fl.swapLines((input1 - 1), (input2 -1) ); //change both inputs -1
				break;
			case 2:
				System.out.println("Line of first word:");
				int firstLinetoSwap = input.nextInt();
				System.out.println("Index of first word:");
				int firstWordIndexToSwap = input.nextInt();
				System.out.println("Line of second word:");
				int secondLineToSwap = input.nextInt();
				System.out.println("Index of Second word:");
				int secondWordIndexToSwap = input.nextInt();
				fl.swapWordsFromIndex(firstLinetoSwap, secondLineToSwap, firstWordIndexToSwap, secondWordIndexToSwap);
				fl.printFile();
				break;
				
			default:
				System.out.println("Invalid Input.");
		} */
		
		
		
		

	}

}
